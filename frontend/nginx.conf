server {
    listen 80;

    location /api {
        proxy_pass http://backend:17001;
    }

    location /media {
        alias /data/eartalk/media;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }
}
